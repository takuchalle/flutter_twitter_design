import 'package:flutter/material.dart';

class MyDirectMailScreen extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Text('Index 3: Direct Mail');
  }
}